workflows:
  testFlight:
    name: "Ios Release"
    integrations:
      app_store_connect: ZOUHAIR MOUFARAJ
    environment:
      flutter: 3.41.0
      ios_signing:
        provisioning_profiles:
          - provisioning_SockslitePro
        certificates:
          - Certificate_zouhir
    scripts:
      - name: Set up code signing settings on Xcode project
        script: xcode-project use-profiles
      - name: Get Flutter packages
        script: flutter packages pub get
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build IPA
        # Set ADMOB_UNITS_JSON_URL in Codemagic Environment variables (HTTPS JSON).
        script: flutter build ipa --release --dart-define=ADMOB_UNITS_JSON_URL="${ADMOB_UNITS_JSON_URL}" --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - moufarijamix50@gmail.com
        notify:
          success: true
          failure: true
      app_store_connect:
        auth: integration


